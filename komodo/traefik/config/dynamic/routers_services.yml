http:
  routers:
    komodo:
      entryPoints:
        - "https"
      rule: "Host(`komodo.prod.darksaber.fyi`)"
      middlewares:
        - secured-external
      tls:
        options: default
      service: komodo
  services:
    komodo:
      loadBalancer:
        servers:
          - url: "http://10.0.100.108:9120"
        passHostHeader: true