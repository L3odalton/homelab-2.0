http:
  routers:
    portainer:
      entryPoints:
        - "https"
      rule: "Host(`portainer.swarm.darksaber.fyi`)"
      middlewares:
        - secured
      tls:
        options: default
      service: portainer
  services:
    portainer:
      loadBalancer:
        servers:
          - url: "https://10.0.100.141:9443"
        passHostHeader: true