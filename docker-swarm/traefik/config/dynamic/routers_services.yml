http:
  routers:
    portainer:
      entryPoints:
        - "https"
      rule: "Host(`portainer.swarm.darksaber.fyi`)"
      middlewares:
        - secured
      tls:
        options: default
      service: portainer
  services:
    portainer:
      loadBalancer:
        servers:
          - url: "http://10.0.100.141:9000"
        passHostHeader: true