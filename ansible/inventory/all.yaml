all:
  hosts:
    srv-management:
      ansible_host: srv-management.local.darksaber.fyi
    srv-dns-02:
      ansible_host: srv-dns-02.local.darksaber.fyi
    localhost:
      ansible_host: localhost
      connection: local

  children:
    komodo:
      vars:
        enable_server_management: true
        komodo_core_url: "https://komodo.darksaber.fyi"
        generate_server_passkey: true
      children:
        core:
          hosts:
            srv-management:
              server_name: "srv-management"
              server_address: https://host.docker.internal:8120
              komodo_bind_ip: "{{ ansible_docker0.ipv4.address}}"
              root_dir: "{{ komodo_home }}/.komodo"
              repo_dir: "{{ root_dir }}/repos"
              stack_dir: "{{ root_dir }}/stacks"
              build_dir: "{{ root_dir }}/build"
        periphery:
          hosts:
            srv-dns-02:
              server_name: "srv-dns-02"
              server_address: "https://srv-dns-02.local.darksaber.fyi:8120"
              komodo_bind_ip: "{{ ansible_host }}"
              root_dir: "{{ komodo_home }}/.komodo"
              repo_dir: "{{ root_dir }}/repos"
              stack_dir: "{{ root_dir }}/stacks"
              build_dir: "{{ root_dir }}/build"